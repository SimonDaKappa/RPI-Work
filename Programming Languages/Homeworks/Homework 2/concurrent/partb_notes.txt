After act_A()

Send each worker the list of other workers, or set up middleman communication of 
worker -> request external degrees from graphstat -> request external degrees from
every other worker -> send degrees back to original worker

With worker[] :
  Init worker[] on each worker, await all workers to finish
  Construct array of all external nodes on each worker
  For each external n, message every other worker asking for its degrees
  Return a list of all max degree nodes (interal and external)


